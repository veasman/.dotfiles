set -ga terminal-overrides ",screen-256color*:Tc"
set-option -g default-terminal "screen-256color"
set -s escape-time 0

unbind C-b
set-option -g prefix C-g
bind-key C-g send-prefix

bind r source-file ~/.tmux.conf
set -g base-index 1

set-window-option -g mode-keys vi
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

# vim-like pane switching
bind -r ^ last-window
bind -r k select-pane -U
bind -r j select-pane -D
bind -r h select-pane -L
bind -r l select-pane -R

# blazingly fast window switching
bind-key -n M-1 switch-client -t :1
bind-key -n M-2 switch-client -t :2
bind-key -n M-3 switch-client -t :3
bind-key -n M-4 switch-client -t :4
bind-key -n M-5 switch-client -t :5

bind -r D neww -c "#{pane_current_path}" "[[ -e TODO.md ]] && nvim TODO.md || nvim ~/.dotfiles/personal/todo.md"

# bind-key -r f run-shell "tmux neww ~/.local/bin/tmux-sessionizer"
bind-key -r f run-shell "~/.local/bin/tmux-sessionizer"

# Runner
bind-key -r r run-shell "~/.local/bin/tmux-runner"
bind-key -r R run-shell "~/.local/bin/tmux-runner --multi"

# Cheat sheet
bind-key -r c run-shell "~/.local/bin/tmux-cheat"

# Styling
set -g status-style 'bg=#333333 fg=#5eacd3'
set -g status-left-length 20
