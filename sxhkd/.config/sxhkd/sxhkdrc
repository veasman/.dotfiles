# Ubuntu may have a screenshot tool
#@Print
    #flameshot gui

# Set a wallpaper at random from a given dir
alt + w
    setbg ~/.local/wallpapers/normal/

# Open an emacs client, or open emacs if no daemon running
alt + e
    emacsclient -c -a 'emacs'

# Open an emacs shell instance
# This will most likely never be uncommented
#alt + t
    #emacsclient -c -a '' --eval '(eshell)'

# Open a new instance of emacs
#alt + shift + t
    #emacs
